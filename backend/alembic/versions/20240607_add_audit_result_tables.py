"""Alembic migration script to create tables for persisting audit analysis results.

Revision ID: 20240607_add_audit_result_tables
Revises: None
Create Date: 2026-06-07 15:20:00.000000
"""

from alembic import op
import sqlalchemy as sa

# revision identifiers, used by Alembic.
revision = "20240607_add_audit_result_tables"
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table(
        "audit_results",
        sa.Column("id", sa.Integer(), primary_key=True, index=True),
        sa.Column("audit_session_id", sa.Integer(), sa.ForeignKey("audit_sessions.id", ondelete="CASCADE"),
        sa.Column("variance_json", sa.JSON(), nullable=True),
        sa.Column("primary_causes_json", sa.JSON(), nullable=True),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    op.create_table(
        "finding_records",
        sa.Column("id", sa.Integer(), primary_key=True, index=True),
        sa.Column("audit_session_id", sa.Integer(), sa.ForeignKey("audit_sessions.id", ondelete="CASCADE"),
        sa.Column("finding_json", sa.JSON(), nullable=True),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    op.create_table(
        "faq_records",
        sa.Column("id", sa.Integer(), primary_key=True, index=True),
        sa.Column("audit_session_id", sa.Integer(), sa.ForeignKey("audit_sessions.id", ondelete="CASCADE"),
        sa.Column("question", sa.Text(), nullable=False),
        sa.Column("answer", sa.Text(), nullable=False),
        sa.Column("category", sa.String(), nullable=True),
        sa.Column("relevance_score", sa.Float(), nullable=True),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    op.create_table(
        "chat_messages",
        sa.Column("id", sa.Integer(), primary_key=True, index=True),
        sa.Column("audit_session_id", sa.Integer(), sa.ForeignKey("audit_sessions.id", ondelete="CASCADE"),
        sa.Column("role", sa.String(), nullable=False),
        sa.Column("content", sa.Text(), nullable=False),
        sa.Column("timestamp", sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_table("chat_messages")
    op.drop_table("faq_records")
    op.drop_table("finding_records")
    op.drop_table("audit_results")
    # ### end Alembic commands ###
